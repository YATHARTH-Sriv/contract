pub const BASE_REWARD_PER_MINUTE: u64 = 1_000_000; // 1 YAP per minute (6 decimals)
pub const HOST_REWARD_PER_MINUTE: u64 = 5_000_000; // 5 YAP per minute for hosting
pub const LISTENER_BONUS_PER_HOST: u64 = 100_000;  // 0.1 YAP bonus per listener for host
pub const MAX_ROOM_DURATION: i64 = 8 * 60 * 60;    // 8 hours max
pub const MIN_ROOM_DURATION: i64 = 5 * 60;         // 5 minutes min